from src import Main

# import pandas as pd
# import math

if __name__ == "__main__":
    Main().start()
    # data = [0.01, -0.01, 0.0, 0.02, -0.02]
    # df = pd.DataFrame({"data": data})
    # print("他的標準差", df["data"].std())

    # data_len = len(data)
    # sum = 0
    # for item in data:
    #     sum += math.pow(item, 2)
    # std = math.sqrt(sum / data_len)
    # print(std)
